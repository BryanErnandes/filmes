import React from "react";
import { Text, SafeAreaView } from "react-native";

export default function Sobre() {
    return (
        <SafeAreaView>
            <Text>Login</Text>
        </SafeAreaView>
    )
}